# task-service — PRODUCTION environment variables
# Copy to .env on EC2 and fill in real values before running docker-compose.prod.yml

PORT=3002
DATABASE_URL=postgresql://app_user:CHANGE_ME_DB_PASSWORD@db-task:5432/tasks_db
